Convert my existing Django backend project "TicketFlix" into a full-stack application with a separate React frontend.

Keep all existing Django models, APIs, and booking logic intact.

Create a React frontend inside the same Replit workspace using Vite.

Architecture:
- Backend: Django REST Framework (existing)
- Frontend: React (Vite)
- Communication: REST APIs
- Use axios for API calls
- Enable CORS in Django

========================================
BACKEND CHANGES (Django)
========================================

1. Install and configure django-cors-headers.
2. Allow frontend origin in CORS settings.
3. Keep all APIs intact:
   - /api/movies/
   - /api/theaters/
   - /api/theaters/shows/
   - /api/bookings/
   - /api/bookings/{id}/cancel/
   - /api/theaters/shows/{id}/available_seats/

4. Ensure booking uses atomic transaction.
5. Ensure cancellation works only if 24 hours remain before show_time.
6. Enable authentication via token or session auth.

========================================
FRONTEND (React + Vite)
========================================

1. Create React app using Vite.
2. Install:
   - react-router-dom
   - axios
   - framer-motion (for animations)
   - bootstrap (or tailwind)
3. Create folder structure:

src/
  components/
  pages/
  services/
  App.jsx
  main.jsx

========================================
PAGES TO IMPLEMENT
========================================

1. Home Page
- Fetch movies from /api/movies/
- Display movie cards
- Add smooth fade-in animation using framer-motion
- Add "View Shows" button

2. Shows Page (/movie/:id)
- Fetch shows for selected movie
- Display theater name, city, show time, price
- Add slide-in animation

3. Seat Selection Page (/show/:id)
- Fetch available seats endpoint
- Display seats in grid layout
- Available seats (green)
- Booked seats (red, disabled)
- Allow multi-select
- Animated seat selection effect
- On submit → call booking API

4. Booking Confirmation Page
- Display booking summary
- Animated success checkmark
- Show cancel button if eligible

5. My Bookings Page
- Fetch user's bookings
- Display booking cards
- Add cancel button
- Cancel via /api/bookings/{id}/cancel/

6. Login & Signup Pages
- Form UI with modern animations
- Store auth token in localStorage
- Protect booking routes

========================================
UI DESIGN REQUIREMENTS
========================================

- Dark cinema theme
- Smooth page transitions
- Animated buttons
- Hover effects
- Seat selection animation
- Clean responsive layout
- Mobile responsive

========================================
IMPORTANT
========================================

- Do not break existing backend logic.
- Ensure React runs on port 5173.
- Backend runs on 8000.
- Use proxy setup if needed.
- Make the project runnable immediately.

Final result:
A fully functional TicketFlix full-stack web app with animated React UI and Django backend.